int GListDepth(GList &L)
{
    int Depth=0;
    int HDepth,TDepth;
    if(!L)return Depth;
    if(L->tag==ATOM)return Depth;
    else{
        Depth++;
        HDepth=Depth+GListDepth(L->hp);
        TDepth=Depth+GListDepth(L->tp);
        return HDepth>TDepth?HDepth:TDepth;
    }
}
